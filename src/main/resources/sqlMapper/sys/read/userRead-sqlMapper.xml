<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.gqhmt.sys.mapper.read.UserReadMapper">
	<sql id="field">
		SELECT
			t.login_name loginName,
			t.user_name userName,
			t.password password,
			t.sex sex
		FROM `t_sys_users` t
	</sql>

	<select id="getUser" parameterType="java.util.Map"  resultType="SysUsers">
		<include refid="field"/>
		WHERE t.login_name =${loginName}
		AND t.password=${loginPwd}
	</select>
</mapper>
