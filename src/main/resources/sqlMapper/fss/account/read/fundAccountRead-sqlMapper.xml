<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.gqhmt.fss.architect.account.mapper.read.FundAccountReadMapper">
	<!-- <sql id="fieldSql">
	</sql>
	<sql id="whereSql">
	</sql>
	<sql id="otherSql">
		<choose>
		<when test="orderStr != null">
			order by ${orderStr}
		</when>
		<otherwise>
		</otherwise>
		</choose>
	</sql> -->
	<select id="queryFundsAccountList" parameterType="fundAccountEntity" resultType="fundAccountEntity">
		SELECT t.account_no FROM 't_gq_fund_account' t;
	</select>
</mapper>
