<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.gqhmt.fss.architect.loan.mapper.read.FssLoanReadMapper">

	<sql id="whereSql">
			<if test="mchnNo != null and mchnNo != ''">
				and t.mchn_child = #{mchnNo}
			</if>
			<if test="seqNo != null and seqNo != ''">
				and t.seq_no = #{seqNo}
			</if>
	</sql>
	<!-- 得到借款人放款回调对象 -->
	<select id="getResponse"  parameterType="java.util.Map" resultType="com.gqhmt.extServInter.dto.loan.LendingResponse" >
		SELECT 
			t.id,
		    t.mchn_child mchn,
		    t.seq_no seq_no,
		    t.trade_type trade_type,
		    t.contract_id contract_id,
		    t.contract_no contract_no,
		    t.mortgagee_acc_no mortgagee_acc_no,
		    t.acc_no acc_no,
			t.contract_amt contract_amt,
			t.pay_amt pay_amt,
		    t.rep_code resp_code,
			t.rep_msg resp_msg,
			t.loan_platform loan_platform
			FROM t_gq_fss_loan t
			where 1=1
			<include refid="whereSql" />
	</select>
	<!-- 抵押权人提现回盘 -->
	<select id="getMortgageeWithDrawRespons"  parameterType="java.util.Map" resultType="com.gqhmt.extServInter.dto.loan.MortgageeWithDrawRespons" >
		SELECT 
			t.id id,
		    t.seq_no seq_no,
		    t.mchn_child mchn,
		    t.trade_type trade_type,
		    t.contract_id contract_id,
		    t.contract_no contract_no,
		    t.acc_no acc_no,
			t.pay_amt pay_amt,
			t.contract_amt contract_amt,
			t.rep_code resp_code,
			t.rep_msg resp_msg
			FROM t_gq_fss_loan t 
			where 1=1
			<include refid="whereSql" />
	</select>
	<!-- 流标回盘回盘 -->
	<select id="getFailedBidResponse"  parameterType="java.util.Map" resultType="com.gqhmt.extServInter.dto.loan.FailedBidResponse" >
		SELECT 
			t.id,
		    t.seq_no seq_no,
		    t.mchn_child mchn,
		    t.trade_type trade_type,
		    t.contract_id contract_id,
		    t.contract_no contract_no,
		    t.mortgagee_acc_no mortgagee_acc_no,
		    t.acc_no acc_no,
			t.contract_amt contract_amt,
			t.pay_amt pay_amt,
		    t.rep_code resp_code,
			t.rep_msg resp_msg,
			t.loan_platform loanPlatform
			FROM t_gq_fss_loan t
			where 1=1
			<include refid="whereSql" />
	</select>
		<!-- 借款人放款列表 -->
	<select id="findBorrowerLoan"  parameterType="java.util.Map" resultType="com.gqhmt.fss.architect.loan.entity.FssLoanEntity" >
		SELECT 
			t.id id,
		    t.contract_id contractId,
		    t.contract_no contract_no,
		    t.acc_no accNo,
			t.contract_amt contractAmt,
			t.rep_code repCode,
			t.seq_no seqNo,
			t.mortgagee_acc_no mortgageeAccNo,
			t.create_time createTime,
			t.modify_time modifyTime,
			t.mchn_parent mchnParent,
			t.mchn_child mchnChild,
			t.result result,
			t.trade_type tradeType,
			t.status status,
			t.rep_msg repMsg,
			t.pay_amt payAmt,
			t.loan_platform loanPlatform  	
			FROM t_gq_fss_loan t 
			where 1=1
			<if test="contractNo != null and contractNo != ''">
				and t.contract_no like concat('%',#{contractNo},'%')
			</if>
			<if test="seqNo != null and seqNo != ''">
				and t.seq_no like concat('%',#{seqNo},'%')
			</if>
			<if test="status != null and status != ''">
				and t.status like concat('%',#{status},'%')
			</if>
			<if test="mchnChild != null and mchnChild != ''">
				and t.mchn_child like concat('%',#{mchnChild},'%')
			</if>
			<if test="startTime != null and startTime != '' ">
				and date_format(t.create_time,'%Y%m%d') &gt;= #{startTime}
			</if>
			<if test="endTime != null and endTime != '' ">
				and date_format(t.create_time,'%Y%m%d') &lt;= #{endTime}
			</if>
			<if test="type !=null and type != '' ">
				AND t.trade_type = #{type}
			</if>

			<!--<choose>
				<when test="creatTime == null or creatTime == '' and modifyTime==null or modifyTime=='' ">
					AND 1=1
				</when>
				<when test="creatTime != null and creatTime != '' and modifyTime==null or modifyTime=='' ">
					AND t.create_time BETWEEN #{creatTime} AND SYSDATE()
				</when>
				<when test="creatTime != null and creatTime != '' and modifyTime!=null and modifyTime!=''">
					AND t.create_time BETWEEN #{creatTime} AND #{modifyTime}
				</when>
				<when test="creatTime == null or creatTime == '' and modifyTime!=null and modifyTime!=''">
					AND t.create_time &lt; #{modifyTime} 
				</when>
			</choose>-->
	</select>

	<select id="findLoanBySettle" resultType="com.gqhmt.fss.architect.loan.entity.FssLoanEntity">
		SELECT
			t.id,
		    t.contract_id contract_id,
		    t.mortgagee_acc_no mortgagee_acc_no,
		    t.acc_no acc_no,
		    t.rep_code resp_code,
			t.rep_msg resp_msg,
			t.contract_amt contract_amt,
			t.pay_amt pay_amt,
			t.loan_platform loan_platform
		FROM t_gq_fss_loan t
		WHERE t.trade_type = 11090002 OR t.trade_type = 11092001) AND t.status = 10050001
	</select>
</mapper>
